#!/bin/bash
# Script to install GNOME Extension Manager on Ubuntu Linux

# Install GNOME Extension Manager via Snap package manager
sudo snap install gnome-extension-manager

# Install GNOME Shell Extension Manager via apt package manager
sudo apt-get install gnome-shell-extension-manager
